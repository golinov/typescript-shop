import React from 'react';

export interface LoginProps {
    email: string;
    password: string;
}

const Auth: React.FC<LoginProps> = (credentials) => {
    return (
        <div>

        </div>
    );
};

export default Auth;