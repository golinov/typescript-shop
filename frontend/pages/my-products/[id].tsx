import React from 'react';
import MainLayout from "../../layouts/MainLayout";

const MyProductPage = ({id}) => {
    return (
        <MainLayout>
            this is detail product page, id is {id}
        </MainLayout>
    );
};

export default MyProductPage;
